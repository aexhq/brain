[package]
name = "brain-contracts"
version = "0.1.0"
description = "Writes the published contracts from the Rust types that define them"
publish = false
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
brain-http = { workspace = true }
brain-protocol = { workspace = true }
serde_json = { workspace = true }
serde_norway = { workspace = true }
