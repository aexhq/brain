---
title: Introduction
description: Brain's durable session engine and the four things that plug into it.
---

Brain runs agent sessions. It owns one canonical journal per session, activates an Agentloop,
executes the model and Tool effects the loop requests, and exposes journal projections over HTTP
and SSE.

Run it as a server your app talks to over HTTP, or embed the `brain` crate in a Rust service you
already own.

<Callout type="warn">
Brain is under early development. Contracts are replaced in place until the first stable release,
and there is no upgrade path from earlier builds.
</Callout>

## The four parts

| Part | You supply | Brain does |
| --- | --- | --- |
| [Agentloop](/brain/docs/concepts/agent-loop) | A raw WebAssembly Component and an Environment | Activates it and performs its model and Tool requests |
| [Model](/brain/docs/concepts/model) | A provider, model name, and credential | Seals the binding for the session and makes calls |
| [Tool](/brain/docs/concepts/tool) | A typed contract plus resident code or a placed implementation | Commits the call before dispatch and records its outcome |
| [Environment](/brain/docs/concepts/environment) | An immutable placement and authority specification | Creates, attaches, invokes, cancels, detaches, and closes it |

Application code composes these values explicitly. `component(...)` wraps already-built Wasm,
`brainWasm(...)` selects Brain's built-in Wasmtime Environment, and placed factories take
`{ env, ...options }`.

## The durable rule

Brain commits every external-effect intent to the session journal before dispatch and sends it
once. It never automatically retries an effect. A known result, a known failure, or an unknown
outcome is committed before the Agentloop observes it.

The public event feed, session status, transcript, and Agentloop slots are projections of the same
journal. SSE is a live notification path; reconnecting clients resume from a committed sequence.

Resident Tools are the deliberate exception to placed isolation: their code remains in the
application process. The SDK registers one resident host, receives Tool commands over SSE, and
posts results and `ctx.emit` events through scoped host endpoints.

## Where to go next

- [Quickstart](/brain/docs/quickstart) — run a server and create a first session.
- [Sessions](/brain/docs/concepts/sessions) — journal, turns, suspension, and recovery.
- [Agentloop](/brain/docs/concepts/agent-loop) — raw Components and explicit placement.
- [Tools](/brain/docs/concepts/tool) — resident and placed implementations.
- [API reference](/brain/docs/reference/api) — generated from the protocol types.
