{
  "name": "bash",
  "description": "Run a shell command in the session workspace.",
  "input_schema": {
    "type": "object",
    "properties": {
      "command": {
        "type": "string"
      }
    },
    "required": [
      "command"
    ]
  },
  "output_schema": {
    "type": "object"
  },
  "needs": [
    "process"
  ],
  "binding_names": [
    "API_BASE"
  ],
  "implementation": {
    "artifact_id": "artifact_bash_v1"
  }
}
